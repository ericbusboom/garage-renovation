# Data

- `frame-models/` contains COMPAS structural frame JSON files using the existing naming scheme.
- `model/` contains the project model, parameters, exchange files, and related model data.

Keep stable data filenames when source code or report provenance records refer to them. Put generated HTML and presentation outputs in `viz/` instead.
